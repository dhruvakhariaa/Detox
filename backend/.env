# Environment Variables for Detox Backend

# Server Port - Using 5001 to avoid conflicts
PORT=5001

# MongoDB Connection URI
MONGO_URI=mongodb://localhost:27017/detox-db

# JWT Secret Key for token signing (we will use this in a later step)
JWT_SECRET=your-super-secret-and-long-jwt-key

# CORS Origin - The URL of your React frontend
CORS_ORIGIN=http://localhost:3000
